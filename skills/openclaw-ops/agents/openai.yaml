display_name: OpenClaw Ops
short_description: Specialize OpenClaw for service incidents, logs, config checks, and safe remediation.
default_prompt: Use the OpenClaw Ops skill to inspect runtime state, logs, config, apply the smallest safe fix, and validate the result.
