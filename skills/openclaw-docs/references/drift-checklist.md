# Drift Checklist

Check for these common documentation drift points:
- outdated install or run commands
- wrong package manager or script names
- missing env/config prerequisites
- paths or filenames that no longer exist
- setup steps that no longer match implementation
- claims about features not reflected in code/config
- release/version notes inconsistent with repository state
